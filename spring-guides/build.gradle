tasks.register('clean') {
    dependsOn gradle.includedBuild('consuming-web-service').task(':clean')
    dependsOn gradle.includedBuild('producing-web-service').task(':clean')
    dependsOn gradle.includedBuild('uploading-files').task(':clean')
    dependsOn gradle.includedBuild('validating-form-input').task(':clean')
    dependsOn gradle.includedBuild('messaging-stomp-websocket').task(':clean')
    dependsOn gradle.includedBuild('authenticating-ldap').task(':clean')
    dependsOn gradle.includedBuild('jpa').task(':clean')
    dependsOn gradle.includedBuild('async-method').task(':clean')
    dependsOn gradle.includedBuild('caching').task(':clean')
    dependsOn gradle.includedBuild('batch-processing').task(':clean')
}
