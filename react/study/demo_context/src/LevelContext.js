import { createContext } from 'react';

//Create the context
export const LevelContext = createContext(1);
export const LevelContext2 = createContext(0);

export const ImageSize=createContext(100);
export const PlaceContext=createContext('');
